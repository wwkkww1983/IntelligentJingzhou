<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/recycler_layout"
    android:layout_width="match_parent"
    android:layout_height="130dp"
    android:background="@drawable/ac_main_bg_line">

    <TextView
        android:id="@+id/tv_item_gun_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:text="正常在库"
        android:textSize="@dimen/text_size_14sp" />

    <TextView
        android:id="@+id/tv_item_cab_no"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:text="位置"
        android:textSize="@dimen/text_size_14sp" />

    <ImageView
        android:id="@+id/iv_item_gun_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:src="@drawable/short_gun"/>

    <TextView
        android:id="@+id/tv_item_gun_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:singleLine="true"
        android:textColor="@color/white"
        android:text="64手枪"
        android:layout_above="@+id/tv_item_gun_count"
        android:layout_marginBottom="@dimen/dimen_3dp"
        android:textSize="@dimen/text_size_14sp" />

    <TextView
        android:id="@+id/tv_item_gun_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:gravity="start"
        android:maxLines="1"
        android:textColor="@color/white"
        android:textSize="@dimen/text_size_14sp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="@dimen/dimen_3dp"
        android:text="数量：111"
        />

    <ImageView
        android:id="@+id/iv_item_gun_check"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:src="@drawable/item_recycler_view_check_64"
        android:visibility="invisible" />

</RelativeLayout>